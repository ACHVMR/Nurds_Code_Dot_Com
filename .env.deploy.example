# Deploy Integration Environment Variables

## Required for Deploy Features

### Deploy Orchestrator Connection
DEPLOY_ORCHESTRATOR_URL=http://localhost:8000
DEPLOY_ORCHESTRATOR_KEY=your-api-key-here

### Supabase (shared Testing Lab data)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE=your-service-role-key

### V.I.B.E. Quality Gates
VIBE_THRESHOLD=0.90

### Feature Flags
REACT_APP_DEPLOY_ENABLED=false

## Production Setup Notes
- DEPLOY_ORCHESTRATOR_KEY: Store in GitHub Secrets and Cloudflare Workers secrets
- SUPABASE_SERVICE_ROLE: Server-side only, never expose to client
- VIBE_THRESHOLD: Set to 0.90 or higher for production quality gates
- REACT_APP_DEPLOY_ENABLED: Set to 'true' to enable Deploy routes (or limit to superadmins)

## Security
- All API keys must be rotated quarterly
- Use RLS policies on Supabase for Testing Lab data
- Deploy Orchestrator should validate x-api-key on all endpoints
- Tracing headers (x-trace-id) correlate Charter/Ledger logs
